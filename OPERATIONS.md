# 高等教育ニュースポータル 運用・保守手順書

## 1. この手順書の目的

この手順書は、高等教育ニュースポータルを継続して運用するために、日常的に行う確認、
大学ジャーナルオンラインのPC補完、障害時の対応、バックアップ、アプリ改修の手順を
まとめたものです。

公開サイト：<https://kobe-higher-education-news.streamlit.app>

GitHub：<https://github.com/ponponponkichi/kobe-higher-education-news>

GitHub Actions：<https://github.com/ponponponkichi/kobe-higher-education-news/actions>

## 2. 通常時の運用周期

| 頻度 | 確認・作業内容 |
|---|---|
| 毎日 | 原則として毎朝8時頃の自動更新に任せる |
| 週1～3回 | PCから大学ジャーナルオンラインを補完する |
| 週1回 | GitHub Actionsのログで一部情報源の失敗がないか確認する |
| 月1回 | 日付、分類、重複、リンク、有料記事の混入を軽く確認する |
| 月1回 | Google Analyticsで利用状況と操作イベントを確認する |
| 数か月に1回 | SQLiteとプロジェクト一式を別の場所へバックアップする |
| アプリ改修時 | PC版で確認後、テスト、Pull Request、公開の順に進める |

## 3. 毎朝8時頃の自動更新

### 3.1 通常の流れ

GitHub Actionsの `Daily higher education news update` が、毎日23:00 UTC
（日本時間の翌朝8時頃）に実行されます。

1. GitHub上でPython 3.12を準備
2. ニュースを収集
3. 公開用JSONを生成
4. 変更がある場合だけ `02_output/public_news.json` をGitHubへ保存
5. Streamlit Community Cloudが更新を読み込む

通常は手作業を行いません。

### 3.2 簡単な確認方法

公開サイト上部の「最終データ更新」を確認します。前日から変わっていない場合は、
GitHub Actionsを開き、最新の実行結果を確認します。

- 緑色のチェック：処理全体は完了
- 赤色の×：処理が途中で失敗
- 黄色または回転中：処理中

日次処理は `--allow-partial` を使用しています。緑色でも一部の情報源が失敗している
場合があるため、週1回程度はログの「情報源○件、失敗○件」を確認します。

## 4. 大学ジャーナルオンラインのPC補完

### 4.1 補完が必要な理由

2026年8月16日現在、GitHub Actionsから大学ジャーナルオンラインのRSSへアクセスすると、
HTTP 403で拒否されます。PCからは取得できるため、PC経由で公開用JSONを補完します。

将来GitHub Actionsから正常取得できるようになった場合は、この補完が引き続き必要かを
改めて確認します。

### 4.2 実行時期

GitHub Actionsとの競合を避けるため、朝8時30分以降に実行します。

- 重要度を優先する場合：平日1日1回
- 負担を抑える場合：週1～3回

### 4.3 実行手順

1. エクスプローラーで `03_scripts` フォルダを開く
2. `publish_university_journal.cmd` をダブルクリック
3. 最初の確認で半角大文字の `Y` を入力してEnter
4. 次の内容を確認
   - GitHubに現在ある記事数
   - PCで取得したRSS記事数
   - 今回新たに追加する記事数
   - 追加候補の記事タイトル
5. 候補を公開してよい場合だけ、半角大文字で `PUBLISH` と入力してEnter
6. 構文検査と単体テストがすべて `OK` になることを確認
7. `Completed` と公開サイトURLが表示されたら完了

候補が0件の場合は、GitHubを更新する必要はありません。

### 4.4 安全設計

次の場合、公開用JSONを変更せずに停止します。

- `Y` または `PUBLISH` 以外を入力した
- RSSを取得できなかった
- 取得件数が0件または想定外に多かった
- 構文検査または単体テストが失敗した
- プレビュー後にGitHubの公開用JSONが別の処理で更新された

新規補完記事では、本文やRSS概要を公開しません。タイトル、媒体名、公開日、分類、
元記事へのリンクだけを追加します。

PC補完はSQLiteを更新しません。公開用JSONは既存記事を引き継いで生成するため、
補完記事は翌朝のGitHub Actions更新後も保持されます。

## 5. データの所在

### 5.1 PC内のSQLite

`02_output/higher_education_news.db`

- 収集した記事の履歴
- 公開概要
- 媒体と取得方式
- 公開日時、初回取得日時、最終確認日時
- 分類、テーマ、関連度、有料記事推定
- 情報源ごとの取得結果とエラー

記事本文や有料部分は保存しません。SQLiteはGitHubへ登録しません。

### 5.2 GitHub上の公開用JSON

`02_output/public_news.json`

- 高等教育との関連度がある記事
- 無料・有料の推定結果にかかわらず掲載
- タイトル、媒体、日付、分類、元記事URL
- 公開してよい概要だけ

Streamlit公開サイトはSQLiteへ接続せず、このJSONだけを読み込みます。

## 6. Google Analyticsの確認

### 6.1 基本設定

- アナリティクスアカウント：高等教育ニュースポータル
- プロパティ：高等教育ニュースポータル
- 測定ID：G-L6PJED9H5W
- イベントデータの保持：14か月
- 新しいアクティビティでのユーザーデータのリセット：オフ
- Googleシグナルと広告パーソナライズ：オフ

測定IDは公開識別子ですが、別のプロパティへデータを送らないよう、変更する場合は
Google Analyticsのデータストリームと03_scripts/app.pyの両方を確認します。
PC版のlocalhostは計測対象外です。

### 6.2 通常の確認

1. 公開サイトを開く
2. Google Analyticsで「レポート」→「リアルタイム」を開く
3. page_viewが届いていることを確認
4. テーマや主語を変更し、theme_filter_changedや
   subject_filter_changedが届くことを確認

通常レポートと探索には24～48時間後に反映される場合があります。GA4には
filter_value、selection_count、has_search_textのカスタム定義を登録しています。

### 6.3 計測する情報と計測しない情報

閲覧、セッション、スクロール、外部リンクのクリック、テーマ・主語・期間の変更、
媒体の選択数、検索欄が使われたか、NEW一覧の開閉と概要保存を記録します。

検索文字そのもの、氏名、メールアドレス、個々の職員を識別する情報は送信しません。
媒体フィルターでは媒体名ではなく選択数だけを記録します。

## 7. 異常時の対応

### 7.1 公開サイトの更新日時が変わらない

1. GitHub Actionsの最新結果を確認
2. 実行中なら完了まで待つ
3. 赤色の×なら失敗したステップを開く
4. 緑色ならログの「失敗○件」を確認
5. GitHub上の `02_output/public_news.json` の更新日時を確認
6. JSONが更新済みなら、数分待って公開サイトを `Ctrl + F5` で再読み込み

### 7.2 GitHub Actionsが赤色になった

ログの最初のエラーを確認します。

- 一時的な通信障害：時間を置いて `Run workflow` から手動実行
- 特定媒体だけ継続失敗：RSS URL、Webページ構造、利用条件を確認
- Pythonやライブラリのエラー：PC版で再現し、修正とテスト後にPull Requestを作成

原因を確認せず、何度も連続で再実行しないようにします。

### 7.3 大学ジャーナル補完が失敗した

失敗時はGitHubを変更していません。次を確認します。

- PCがインターネットへ接続できるか
- GitHub CLIで認証されているか
- GitHub Actionsが同時実行中ではないか
- 大学ジャーナルのRSSをブラウザで開けるか

競合を示すメッセージの場合は、GitHub Actions完了後に最初から再実行します。

### 7.4 Gitのロックエラーが出た

Google Driveの同期や、途中終了したGit処理により `.git` 内へ一時ロックが残る場合があります。

1. 実行中のGit、PowerShell、Codexの作業がないか確認
2. 対象ファイルのパスが、このプロジェクトの `.git` 内であることを確認
3. 0バイトの一時ロックであることを確認
4. 不明な場合は削除せず、Codexへエラー全文を伝える

確認せずに `.git` 内のファイルを削除しないでください。

### 7.5 不自然な未来日付が表示された

情報源によっては、記事公開日ではなく会議開催日などを日付として配信する場合があります。
単発なら記録し、同じ媒体で繰り返す場合は日付取得ルールを修正します。

## 8. バックアップ

### 8.1 対象

- `02_output/higher_education_news.db`
- `03_scripts`
- `.github`
- `.streamlit`
- `requirements.txt`
- `README.md`
- `OPERATIONS.md`

### 8.2 手順

1. Streamlitを起動しているPowerShellで `Ctrl + C`
2. ニュース更新や大学ジャーナル補完が動いていないことを確認
3. Google Driveの同期完了を確認
4. 対象を日付入りの別フォルダへコピー
5. SQLiteのファイルサイズが0バイトでないことを確認

バックアップから復元する前には、現在のファイルも別の場所へコピーします。

## 9. 情報源と権利への配慮

- 記事本文を保存・転載しない
- 有料部分やログイン後の内容を取得しない
- タイトル、公開概要、日付、媒体、URLを中心に扱う
- 新しい媒体を追加する前に公式RSSと利用条件を確認する
- 新聞・雑誌・Googleニュース経由の記事概要を公開しない
- 公的機関と正式RSSの公開概要だけを慎重に利用する
- PR TIMESとねとらぼを、媒体名または記事タイトルで除外する
- 元記事へのリンクと自動分類である旨を表示する

## 10. 認証情報の管理

次の情報をコード、README、SQLite、公開用JSON、GitHubの通常ファイルへ書きません。

- APIキー
- GitHubアクセストークン
- メールパスワード
- 個人情報や職場内部の情報

必要な場合は、GitHub SecretsまたはStreamlit Secretsを使用します。

## 11. アプリを変更するとき

1. PC側をGitHubの最新 `main` に揃える
2. `main` から作業ブランチを作る
3. PC版で変更する
4. Chrome倍率100％で表示を確認する
5. 構文検査と単体テストを実行する
6. 作業ブランチをGitHubへ送る
7. Pull Requestを作成して内容を確認する
8. `main` へマージする
9. Streamlit公開サイトを確認する
10. PC側も最新 `main` に戻す

主な確認コマンドは次のとおりです。

```powershell
python -m compileall -q .\03_scripts
$env:PYTHONPATH=(Resolve-Path '.\03_scripts').Path
python -m unittest discover -s .\03_scripts\tests -v
python -m streamlit run .\03_scripts\app.py
```

## 12. 定期的に見直す項目

- 情報源のRSS URLや利用条件に変更がないか
- Googleニュース検索の取得が継続できているか
- 有料記事判定に偏りがないか
- 不自然な未来日付や古い記事が増えていないか
- 重複記事が増えていないか
- 主語・テーマ分類に繰り返し同じ誤りがないか
- 公開用JSONの増加で表示速度が低下していないか
- Google Analyticsのリアルタイムで閲覧・操作イベントが届いているか

記事数が増えて表示が重くなった場合は、年度別JSONや古い記事のアーカイブを検討します。

## 13. 迷ったときの原則

- 不明な記事は人手で毎回削除せず、傾向を確認してルールを改善する
- 取得や公開に迷う場合は、公開しない側で一度停止する
- エラー時は既存データを上書きせず、ログと画面を保存する
- 元の状態へ戻せるよう、変更は作業ブランチとPull Requestで行う
- パスワードや認証情報を貼り付けない
